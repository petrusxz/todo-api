This is a REST API running with Node, Express and MongoDB.

## Getting Started

To install node modules, run:

```sh
npm i
```

To start MongoDB database, run:

```sh
mongod
```

To start the server, run:

```sh
npm start
```

To run the tests:

```sh
npm test
```
